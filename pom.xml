<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>
  <groupId>nl.xs4all.banaan.tst8</groupId>
  <artifactId>tst8-app</artifactId>
  <packaging>war</packaging>
  <version>1.0-SNAPSHOT</version>
  <name>wicket-demo</name>
  <description>
    A test to find effective ways to do testing and deployment
    of a wicket application.
  </description>
  
  <scm>
    <url>http://github.com/ekonijn/wicket-demo/tree/master</url>
  </scm>

  <licenses>
    <license>
      <name>The Apache Software License, Version 2.0</name>
      <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <dependencies>
    <!--  WICKET DEPENDENCIES -->

    <dependency>
      <groupId>org.apache.wicket</groupId>
      <artifactId>wicket</artifactId>
      <version>${wicket.version}</version>
    </dependency>
    <!--
      OPTIONAL <dependency> <groupId>org.apache.wicket</groupId>
      <artifactId>wicket-extensions</artifactId>
      <version>${wicket.version}</version> </dependency>
    -->
    <dependency>
      <!-- The spring-web application factory. --> 
      <groupId>org.apache.wicket</groupId>
      <artifactId>wicket-spring</artifactId>
      <version>${wicket.version}</version>
      <!-- exclude spring 2.0 framework that wicket pulls in; clashes with 2.5.6 -->
      <exclusions>
        <exclusion>
          <groupId>org.springframework</groupId>
          <artifactId>spring</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    
    <!-- SPRING DEPENDENCIES -->
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-web</artifactId>
      <version>${spring.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-test</artifactId>
      <version>${spring.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <!-- Interface to java mail; used to be spring-support -->
      <groupId>org.springframework</groupId>
      <artifactId>spring-context-support</artifactId>
      <version>${spring.version}</version>
    </dependency> 

    <!-- LOGGING DEPENDENCIES - LOG4J -->
    <dependency>
      <!--
       ! SL4J is a logging facade used by eg wicket.
       ! The underlying logging mechanism is selected
       ! by depending on the slf4j to log4j mapping jar.
       ! Note that spring uses a similar facade, commons-logging.
       ! commons-logging will delegate to log4j if available.
       ! If you need another destination, let commons-logging
       ! delegate to slf4j; see http://www.slf4j.org/legacy.html
       ! Note that javamail bypasses this logging config; this is because
       ! the mail session is provided by the application container,
       ! not by a jar included by the application.
       !-->
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-log4j12</artifactId>
      <version>1.4.2</version>
    </dependency>
    <dependency>
      <groupId>log4j</groupId>
      <artifactId>log4j</artifactId>
      <version>1.2.14</version>
    </dependency>

    <!--  JUNIT DEPENDENCY FOR TESTING -->
    <dependency>
      <groupId>junit</groupId>
      <!-- for 4.5, junit-dep does not have hamcrest builtin
        but declares a dependency, which simplifies 
        spring-mock integration. for 4.4 junit-dep breaks maven.
        -->
      <artifactId>junit</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>

    <!--  JETTY DEPENDENCIES FOR TESTING  -->
    <dependency>
      <groupId>org.mortbay.jetty</groupId>
      <artifactId>jetty</artifactId>
      <version>${jetty.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.mortbay.jetty</groupId>
      <artifactId>jetty-util</artifactId>
      <version>${jetty.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.mortbay.jetty</groupId>
      <artifactId>jetty-management</artifactId>
      <version>${jetty.version}</version>
      <scope>provided</scope>
    </dependency>
    
    <!--  EASYMOCK FOR TESTING -->
    <dependency>
      <groupId>org.easymock</groupId>
      <artifactId>easymock</artifactId>
      <version>2.4</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <resources>
      <resource>
        <filtering>true</filtering>
        <directory>src/main/templates</directory>
      </resource>
      <resource>
        <filtering>false</filtering>
        <directory>src/main/resources</directory>
      </resource>
      <resource>
        <filtering>false</filtering>
        <directory>src/main/java</directory>
        <includes>
          <include>**</include>
        </includes>
        <excludes>
          <exclude>**/*.java</exclude>
        </excludes>
      </resource>
    </resources>
    <testResources>
      <testResource>
        <filtering>false</filtering>
        <directory>src/test/java</directory>
        <includes>
          <include>**</include>
        </includes>
        <excludes>
          <exclude>**/*.java</exclude>
        </excludes>
      </testResource>
      <testResource>
        <filtering>false</filtering>
        <directory>src/test/resources</directory>
        <includes>
          <include>**</include>
        </includes>
      </testResource>
    </testResources>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <source>1.5</source>
          <target>1.5</target>
        </configuration>
      </plugin>
      <plugin>
        <!-- 
          This plugin allows running the application on port 8080 in a jetty
          container for testing purposes by doing mvn jetty:run.
          The jetty container is embedded within maven.
          The application needs to be compiled but not packaged into a war.
          http://jetty.mortbay.org/jetty/maven-plugin/run-mojo.html
          -->
        <groupId>org.mortbay.jetty</groupId>
        <artifactId>maven-jetty-plugin</artifactId>
        <configuration>
          <jettyEnvXml>src/jetty/jetty-env.xml</jettyEnvXml>
          <overrideWebXml>src/jetty/override-web.xml</overrideWebXml>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <reporting>
    <plugins>
      <plugin>
        <!--  test coverage -->
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>cobertura-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </reporting>

  <properties>
    <!--  spring-test 2.5.6 breaks on junit 4.5
      see http://jira.springframework.org/browse/SPR-5145
     -->
    <junit.version>4.4</junit.version>
    <wicket.version>1.3.5</wicket.version>
    <jetty.version>6.1.4</jetty.version>
    <spring.version>2.5.6</spring.version>
  </properties>
</project>
